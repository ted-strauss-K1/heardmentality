<?php

function tester_boot() {
	
}

function tester_init() {
	
	
}  


function tester_menu() {
  return array(
    'tester' => array(
      'page callback' => 'tester',
      'type' => MENU_CALLBACK,
      'access callback' => true,
    ),
  );
}

function tester() {

  $term = (object)array('tid' => 20);

  $data = node_load(55);
  $data = i18n_language_list();;
//  $data = _theme_build_registry();

  var_dump($data);

  exit;
}

