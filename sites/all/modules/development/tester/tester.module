<?php

function tester_boot() {
	
}

function tester_init() {
	
	
}  


function tester_menu() {
  return array(
    'tester' => array(
      'page callback' => 'tester',
      'type' => MENU_CALLBACK,
      'access callback' => true,
    ),
  );
}

function tester() {

  $data = theme('issue_charts_strength');
//  $data = _theme_build_registry();

  var_dump($data);

  exit;
}