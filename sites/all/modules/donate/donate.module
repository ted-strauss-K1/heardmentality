<?php
/**
 * Implementation of hook_perm().
 */
function donate_perm() {
  return array('Donation','Donor list');
}


/**
 * Implementation of hook_menu().
 */
 
function donate_menu() {	

$donate_result['donation'] = array(
    'title' => ' Donation',
    'page callback' => 'donation_paypal',
    'access arguments' => array('Donation'),
    'type' => MENU_SUGGESTED_ITEM,
    'file' => 'donate.pages.inc',
  );
  
 $donate_result['paypal'] = array(
    'title' => ' Donation',
    'page callback' => 'paypal_process',
    'access arguments' => array('Donation'),
    'type' => MENU_SUGGESTED_ITEM,
    'file' => 'donate.pages.inc',
  );
  $donate_result['paysuccess'] = array(
    'title' => ' Donation',
    'page callback' => 'paypal_success',
    'access arguments' => array('Donation'),
    'type' => MENU_SUGGESTED_ITEM,
    'file' => 'donate.pages.inc',
  );
  $donate_result['payfail'] = array(
    'title' => ' Donation',
    'page callback' => 'paypal_fail',
    'access arguments' => array('Donation'),
    'type' => MENU_SUGGESTED_ITEM,
    'file' => 'donate.pages.inc',
  );
   $donate_result['admin/donorlist'] = array(
    'title' => ' Donation',
    'page callback' => 'donor_list',
    'access arguments' => array('Donor list'),
    'type' => MENU_SUGGESTED_ITEM,
    'file' => 'donate.pages.inc',
  );
  return $donate_result;
}

